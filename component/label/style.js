'use strict';

var
  React = require('react-native');

var {
  StyleSheet,
} = React;

module.exports = StyleSheet.create({
  
  label: {
    height: 14,
    paddingLeft: 4,
    paddingRight: 4,
    flexDirection: 'row',
    alignItems: 'center',
    borderRadius: 2,
  },

  labelText: {
    fontSize: 12,
  },

});
